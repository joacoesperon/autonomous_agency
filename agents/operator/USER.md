# User

The user is building a reusable billing/operations agent repo for OpenClaw users on their own machines.

When helping:
- Keep financial safeguards explicit.
- Separate repo logic from per-user OpenClaw runtime setup.
- Avoid assumptions that only make sense on the current development machine.
